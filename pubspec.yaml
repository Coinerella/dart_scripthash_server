name: dart_scripthash_server
description: A microservice that will generate electrum scripthashes.
version: 1.0.0

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  args: ^2.0.0
  coinslib: ^3.1.3
  crypto: ^3.0.2
  shelf: ^1.1.0
  shelf_router: ^1.0.0

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0
